---
'@sawala/kodena-mcp': minor
---

Bootstrap the `@sawala/kodena-mcp` workspace (M1 of `PLAN-kodena-mcp-server.md`). Ships the stdio MCP server scaffold — empty tools list, `--help` / `--version`, esbuild single-file CJS bundle, vitest smoke tests. The tool surface (read tools, writes, deploy) lands in subsequent milestones.
